class Quad:
     def __init__(self, left, right, left_gripper, right_gripper):
         self.left = left
         self.right = right
         self.left_gripper = left_gripper
         self.right_gripper = right_gripper
